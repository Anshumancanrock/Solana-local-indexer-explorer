/**
 * Minimum length of a search query, shared by the /api/search route and the
 * navbar so the client can never fire a request the server will reject.
 */
export const SEARCH_MIN_QUERY_LENGTH = 3;
